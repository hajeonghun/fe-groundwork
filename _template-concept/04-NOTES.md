# {{TOPIC}} — 정리

> **작성 주체: 🧑 나 / 🤝 AI가 채점**
> **언제:** **반드시 `03-CASES.md` 사례 분석을 마친 뒤.** 이 순서가 이 프로젝트 설계의 핵심이다.
> **목적:** 내 언어로 재구성해서 진짜 이해했는지 확인.
>
> ⚠️ **AI 요약을 붙여넣는 순간 이 파일은 쓰레기가 된다.**
> AI에게 요약을 시키면 기억에 남지 않는다. 요약은 이해의 결과물이지 수단이 아니다.
> **한 글자도 복붙하지 않는다.** 문장이 어설퍼도 내가 쓴 게 낫다.

## 왜 마지막에서 두 번째인가

정리를 먼저 하면 그건 `00-MAP.md`를 옮겨 적는 일이 된다.
질문하고(`01`), 근거를 확인하고(`02`), 실제 사례를 찾은(`03`) **뒤에 쓰는 정리**만 내 것이 된다.

**개념형에서는 아래 「판단에 쓰는 규칙」 섹션이 특히 중요하다.**
설계 주제는 수치가 없어서, 이 규칙 목록이 유일하게 남는 실무 산출물이다.

## 쓰는 방법

### 1) 다른 파일을 전부 닫고 이 파일만 연다

보면서 쓰면 **옮겨 적기**가 된다. 그건 이해가 아니라 편집이다.
기억에서 꺼내는 행위 자체가 학습이고, **꺼내다 막히는 지점이 곧 내 구멍**이다. 열어놓으면 그게 안 보인다.

> **예외:** `03-CASES.md`의 **사례 위치**(어느 모듈, 어느 파일)는 봐도 된다. 위치는 외울 대상이 아니다.
> 다만 **"그 사례가 왜 그렇게 됐는지"의 해석은 보지 않고 다시 꺼낸다.** 거기가 이해가 걸린 부분이다.
> 닫아야 하는 건 `00-MAP.md`·`02-SOURCES.md`처럼 **설명이 적힌 파일**이다.

### 2) 섹션별로 기억에서 쓴다

순서대로 하지 않아도 된다. **「판단에 쓰는 규칙」부터 쓰는 것을 권한다.**
실무에서 꺼내 쓸 형태가 진짜 목표라, 거기서 막히면 나머지가 다 흐릿하다는 신호다.

### 3) 막히면 짜내지 말고 표시만 하고 넘어간다 ★

억지로 채우면 **틀린 기억이 그대로 굳는다.** 비워두는 게 낫다.

```markdown
서버 상태는 원격이 진실이라 캐시 무효화 전략이 필요한데,
[?막힘: 낙관적 업데이트 중 다른 변경이 끼어들면 롤백을 어떻게 하는지]
```

### 4) 다 쓴 뒤 파일을 열어 대조한다

`[?막힘]` 표시한 곳이 진짜 구멍이다.
**그 항목을 `01-QUESTIONS.md`에 먼저 옮겨 적고**, 그다음 본문을 채운다.

### 5) AI에게 채점시킨다

```
내가 이해한 걸 적어볼게. 사실 오류, 빠진 단계, 애매한 표현을 지적해줘.
칭찬은 빼고 틀린 것만.

---
(내 정리 붙여넣기)
```

### 6) 내 문장을 고쳐 쓴다

⚠️ **AI 답변을 아래에 덧붙이지 않는다.** 붙여넣는 순간 이 문서는 내 것이 아니게 된다.

그리고 **지적받은 게 다 맞는 것도 아니다.** 납득이 안 되면 반박한다.

```
그 지적이 이해가 안 돼. 내가 이렇게 생각한 이유는 [...]인데,
그럼에도 틀렸다면 어디가 잘못된 건지 구체적으로 알려줘.
```

여기서 AI가 물러서는 경우도 꽤 있다. **그 판별력이 붙는 것이 이 단계의 진짜 소득이다.**

---

## 한 줄 정의

<!-- 이 주제를 한 문장으로. 여기서 막히면 아직 이해 못 한 것이다. -->

---

## 핵심 메커니즘

<!-- "왜 이렇게 동작하는가"를 내 말로.
     그림이 필요하면 그려서 assets/에 넣고 링크한다. 손그림도 충분하다. -->

---

## 판단에 쓰는 규칙

<!-- ★ 실무에서 실제로 꺼내 쓸 형태로 압축한다.
     "이럴 때는 이렇게 판단한다" 목록.
     이게 이 학습의 최종 산출물이다. 코드 리뷰나 회의에서 즉시 꺼내 쓸 수 있어야 한다.

     ─────────── 작성 예시 ───────────

     | 상황 | 판단 | 근거 |
     | 목록 조회 결과를 여러 화면에서 씀 | 전역 스토어 말고 서버 상태 캐시로 | 진실의 출처를 하나로 유지 |
     | 필터·정렬·페이지 상태 | URL 쿼리스트링에 둔다 | 공유·북마크·뒤로가기가 공짜로 해결됨 |
     | 다른 상태로 계산 가능한 값 | 저장하지 않고 계산한다 | 저장하면 동기화 버그가 생긴다 |

     ★ 나쁜 예: "서버 상태와 클라이언트 상태는 다르다"
        → 사실 서술이지 판단이 아니다. 리뷰에서 꺼내 쓸 수 없다.
     ★ 좋은 예: 위처럼 "이 상황이면 이렇게 한다" 형태.

     ──────────────────────────────── -->

| 상황 | 판단 | 근거 |
|------|------|------|
|  |  |  |

---

## 흔한 오해와 함정

<!-- 02-SOURCES.md의 오개념 수집과 내가 실제로 틀렸던 것을 합쳐서. -->

---

## 막혔던 지점

<!-- 1차 작성에서 [?막힘]으로 표시했던 것들.
     본문은 이미 채웠으므로, 여기는 "무엇을 못 꺼냈는가"의 기록이다.
     같은 곳에서 반복해서 막히면 그건 아직 이해가 아니라 암기다. -->

| 막혔던 것 | 왜 못 꺼냈나 |
|-----------|-------------|
|  |  |

---

## AI 채점 결과

<!-- 지적받은 것 중 실제로 틀렸던 것만. 위 본문은 이미 고쳐 썼다는 전제.
     반박해서 AI가 물러선 것도 적어둘 가치가 있다 — 내 판단이 맞았던 기록이다. -->

| 지적 | 반영 여부 |
|------|-----------|
|  |  |

---

## 인접 주제와의 연결

<!-- 이 주제가 다른 주제와 어디서 만나는가.
     연결이 보이면 지식이 목록이 아니라 구조가 된다. -->

- ← 이 주제를 이해하려면 필요했던 것:
- → 이 주제를 알아서 이해된 것:

---

**작성일:** {{DATE}}
